# Dockerfile placeholder — el Dockerfile real con build multi-stage NestJS viene en T-004.
# Este archivo existe únicamente para que cd-staging.yml pueda hacer docker build
# sin reventar mientras la implementación del API no está lista.
FROM node:20-alpine
