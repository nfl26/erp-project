// PLACEHOLDER — será reemplazado por `prisma db pull` en T-004
// No representa el schema real de Arteo. Ver docs/prisma-workflow.md
//
// Prisma 7: la URL de conexión ya no va aquí.
// Se configura en prisma.config.ts (para la CLI) y vía adapter en PrismaService (runtime).

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["views"]
}

datasource db {
  provider = "postgresql"
}
